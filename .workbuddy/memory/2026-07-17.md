# 2026-07-17 工作日志

## 为斗地主游戏添加背景音乐
- 现有音效系统 `src/game/sounds.ts` 全部用 Web Audio API 程序化生成（无外部音频文件），沿用此风格合成背景音乐。
- 在 `sounds.ts` 末尾新增 `BackgroundMusicController` 类（导出为 `BackgroundMusic` 单例）：
  - C 大调五声音阶（宫商角徵羽），I–vi–ii–V 进行，旋律轨(triangle)+低音轨(sine)
  - 两段交替变奏 pattern，16 步循环，330ms/步 (~91bpm)
  - start() 1.5s 淡入，stop() 0.4s 淡出，复用现有 `getCtx()` 单例 AudioContext
- `GameHeader.tsx` 新增 `musicEnabled` / `onToggleMusic` props，header-right 加圆形音乐开关按钮（音符/静音 SVG 图标）。
- `DoudizhuGame.tsx`：加 `musicEnabled` state(默认 true)，`startGame` 启动 BGM，`toggleMusic` 切换，卸载时 stop。
- `game.css` 加 `.music-toggle` 样式（金色牌馆主题配色）。
- `bun run build` + `bun run lint` 均通过。
